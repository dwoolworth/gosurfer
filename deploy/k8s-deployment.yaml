# gosurfer HumanMode deployment for Kubernetes
# Routes traffic through Tailscale egress proxy on sdinas02 NAS (residential IP)
#
# Prerequisites:
#   1. Tailscale Kubernetes operator with egress to sdinas02
#   2. Chrome profile stored in a PVC or ConfigMap (optional)
#   3. Container image built from deploy/Dockerfile.human
#
# Build & push:
#   docker build -f deploy/Dockerfile.human -t your-registry/gosurfer-human:latest .
#   docker push your-registry/gosurfer-human:latest
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: gosurfer-profile
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: gosurfer-agent
  labels:
    app: gosurfer
spec:
  # Scale horizontally: each replica runs its own browser with its own page
  # pool. Total concurrent browser sessions = replicas * GOSURFER_MAX_PAGES.
  replicas: 3
  selector:
    matchLabels:
      app: gosurfer
  template:
    metadata:
      labels:
        app: gosurfer
    spec:
      containers:
        - name: gosurfer
          image: your-registry/gosurfer-human:latest
          env:
            # Route through Tailscale VPN -> sdinas02 NAS -> residential IP
            - name: GOSURFER_PROXY
              value: "http://sdinas02:3128"
            # Persistent Chrome profile (preserves cookies, login state).
            # NOTE: A ReadWriteOnce PVC cannot be shared by multiple replicas.
            # If scaling beyond 1 replica, either use ReadWriteMany or remove
            # the profile volume and use ephemeral profiles per-pod.
            - name: GOSURFER_PROFILE
              value: "/home/gosurfer/.chrome-profile"
            # HumanMode: system Chrome + new headless + stealth
            - name: GOSURFER_HUMAN
              value: "true"
            # Max concurrent browser pages per pod. Cluster capacity =
            # replicas * this value. Default is 10.
            - name: GOSURFER_MAX_PAGES
              value: "10"
            # Per-tool timeout (supports Go duration syntax).
            - name: GOSURFER_TOOL_TIMEOUT
              value: "90s"
            # LLM API keys (from secrets)
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: gosurfer-secrets
                  key: openai-api-key
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: gosurfer-secrets
                  key: anthropic-api-key
                  optional: true
          volumeMounts:
            - name: chrome-profile
              mountPath: /home/gosurfer/.chrome-profile
            # Chrome needs shared memory for rendering
            - name: dshm
              mountPath: /dev/shm
          ports:
            - containerPort: 8080
              name: mcp
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 15
            periodSeconds: 30
          readinessProbe:
            httpGet:
              path: /healthz
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1.5Gi"
              cpu: "1000m"
          securityContext:
            runAsNonRoot: true
            runAsUser: 1000
            readOnlyRootFilesystem: false
            allowPrivilegeEscalation: false
      volumes:
        - name: chrome-profile
          persistentVolumeClaim:
            claimName: gosurfer-profile
        # Shared memory for Chrome (default 64MB is too small)
        - name: dshm
          emptyDir:
            medium: Memory
            sizeLimit: 256Mi
